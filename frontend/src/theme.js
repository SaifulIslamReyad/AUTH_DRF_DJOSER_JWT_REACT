import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6d6d6dff",
    },
    secondary: {
      main: "#e7e7e7ff",
    },
    tartiary: {
      main: "#000000ff",
    },
    verified: {
      main: "#6c9578ff",
    },
  },
});

export default theme;
